# ⚡ Быстрый старт

## 🎯 За 5 минут до первого запуска

### 1. Установка (30 сек)

```bash
npm install
```

### 2. Настройка .env (2 мин)

```bash
cp .env.example .env
```

Открой `.env` и заполни **только эти 3 поля** для начала:

```env
TELEGRAM_BOT_TOKEN=получи_от_@BotFather
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_KEY=твой_anon_ключ
```

### 3. Настройка Supabase (2 мин)

1. Открой [supabase.com](https://supabase.com) → SQL Editor
2. Скопируй содержимое файла `supabase-setup.sql`
3. Вставь и нажми Run

### 4. Запуск (30 сек)

```bash
npm run dev
```

Открой Telegram → найди своего бота → отправь `/start`

**Готово!** 🎉

---

## 🚀 Деплой на Vercel (5 минут)

### Вариант 1: Через CLI (рекомендуется)

```bash
# Установка
npm i -g vercel

# Логин
vercel login

# Деплой
vercel

# Добавление переменных
vercel env add TELEGRAM_BOT_TOKEN
vercel env add SUPABASE_URL
vercel env add SUPABASE_KEY
vercel env add WEBHOOK_DOMAIN  # твой-проект.vercel.app

# Финальный деплой
vercel --prod
```

### Вариант 2: Через GitHub

1. Загрузи проект на GitHub
2. Зайди на [vercel.com](https://vercel.com)
3. New Project → Import твой репозиторий
4. Добавь переменные окружения
5. Deploy

### Активация вебхука

Открой в браузере:
```
https://твой-проект.vercel.app/api/webhook
```

---

## 📋 Чеклист

- [ ] `npm install` выполнен
- [ ] `.env` создан и заполнен
- [ ] Supabase таблицы созданы
- [ ] `npm run dev` работает
- [ ] Бот отвечает в Telegram
- [ ] Деплой на Vercel выполнен
- [ ] Вебхук активирован

---

## 🆘 Проблемы?

### Бот не отвечает локально

```bash
# Проверь .env
cat .env

# Перезапусти
npm run dev
```

### Бот не отвечает на Vercel

```bash
# Проверь логи
vercel logs

# Проверь переменные
vercel env ls
```

### Ошибка Supabase

1. Проверь URL и ключ в `.env`
2. Убедись, что таблицы созданы (Table Editor)
3. Проверь RLS политики (отключи для теста)

---

## 📚 Дальше

- Полная документация: `README.md`
- Подробный гайд: `SETUP_GUIDE.md`
- SQL аналитика: `supabase-analytics.sql`

---

## 💡 Полезные команды

```bash
# Разработка
npm run dev              # Запуск в dev режиме
npm run build            # Сборка TypeScript

# Vercel
vercel                   # Деплой preview
vercel --prod            # Деплой production
vercel logs              # Просмотр логов
vercel env ls            # Список переменных
vercel domains           # Управление доменами

# Git
git add .
git commit -m "Update"
git push                 # Auto-deploy если подключен Vercel
```

---

## 🎨 Кастомизация

### Изменить текст приветствия

Файл: `src/bot.ts`
```typescript
bot.command('start', async (ctx) => {
  await ctx.reply(
    '👋 Твой текст здесь!',
    mainMenuKeyboard
  );
});
```

### Изменить rate limit

Файл: `src/config.ts`
```typescript
rateLimitRequests: 5,        // 5 запросов
rateLimitWindow: 60000,      // за 1 минуту
```

### Добавить новую кнопку

Файл: `src/keyboards.ts`
```typescript
export const mainMenuKeyboard = Markup.inlineKeyboard([
  [
    Markup.button.callback('🆕 Новая кнопка', 'new_action'),
  ],
  // ...
]);
```

Файл: `src/bot.ts`
```typescript
bot.action('new_action', async (ctx) => {
  await ctx.reply('Обработка новой кнопки!');
});
```

---

**Всё готово к работе!** 🚀
